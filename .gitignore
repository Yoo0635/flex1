# =========================================
# React 프로젝트를 위한 .gitignore
# =========================================

# Dependencies (가장 중요)
# 프로젝트 실행에 필요한 라이브러리들이 담겨있는 폴더입니다.
# 용량이 매우 크고, package.json만 있으면 언제든 다시 설치할 수 있습니다.
/node_modules

# Build output (빌드 결과물)
# 개발용 소스 코드를 웹 브라우저가 읽을 수 있도록 변환한 결과물 폴더입니다.
# 소스 코드만 있으면 언제든 다시 생성할 수 있습니다.
/build
/dist
/.vite
/.output
/.next

# Environment variables (환경 변수 파일 - 보안상 매우 중요!)
# API 키, 데이터베이스 비밀번호 등 민감한 정보를 담고 있는 파일입니다.
# 절대로 GitHub에 올리면 안 됩니다.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs (각종 로그 파일)
# 오류나 실행 기록이 담긴 로그 파일입니다.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE & OS specific files (개발 환경 및 운영체제 자동 생성 파일)
# VS Code, macOS 등에서 자동으로 생성하는 설정 파일들입니다.
.vscode/
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test reports (테스트 결과물)
# 테스트 실행 시 생성되는 결과 보고서입니다.
/coverage